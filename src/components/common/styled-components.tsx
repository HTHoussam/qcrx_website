import { Button, styled } from '@mui/material';

export const MainButton = styled(Button)(() => ({
  backgroundColor: '#194894',
  padding: '12px 18px',
  borderRadius: '24px',
  fontSize: '14px',
  '&:hover': {
    backgroundColor: '#194894',
  },
}));
